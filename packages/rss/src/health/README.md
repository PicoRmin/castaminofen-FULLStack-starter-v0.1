# Feed Health Monitoring Framework

This package provides a reusable, immutable health evaluation layer for RSS feeds.
It intentionally avoids any synchronization, retry, recovery, worker, or scheduler responsibilities.
