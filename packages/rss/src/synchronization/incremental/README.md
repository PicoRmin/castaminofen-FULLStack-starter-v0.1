# Incremental Synchronization Engine

This package provides a reusable incremental synchronization engine for feed updates. It reuses the existing feed-state, checkpoint, comparison, and import abstractions while keeping the logic focused on diff-driven import planning and conditional download handling.
